spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dr
    username: root
    password: Esperer123!

  exposed:
    generate-ddl: true

logging:
  level:
    Exposed: debug